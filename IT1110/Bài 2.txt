2.1
#include <stdio.h> 
int main() 
{ 
    float a,b,tong; 
    scanf("%f",&a); 
    scanf("%f",&b); 
    tong=a+b; 
    printf("%f",tong); 
    return 0; 
}

2.2
#include <stdio.h> 
int main()
{
   float a,b, max;
   scanf("%f",&a);
   scanf("%f",&b);
   max=a;
   if (max <b)  
     max = b;
   printf("%6.2f",max);
   return 0; 
} 
2.3
#include <stdio.h>
int main()
{
float x,y,min;
scanf("%f",&x);
  scanf("%f",&y) ;
  if (x<y)  
  min=x;
  else 
  min=y;
  printf("%6.2f",min);
  return 0;
}
2.4
#include <stdio.h>

int main()
{
  float x,y,max;
  scanf("%f",&x);
  scanf("%f",&y);
  if (x>y)
  max=x;
  else
  max=y;
  printf("%6.2f",max);
  return 0;
}
2.5
#include <stdio.h>
#include <conio.h>
 main()
 {int a,b,c;
  float tong;
  printf("Nhap a:");scanf("%d",&a);
  printf("Nhap b:");scanf("%d",&b);
  printf("Nhap c:");scanf("%d",&c);
  tong= (float) a+b+c;
  printf("%.0f \n",tong);
  printf("%f",tong/3);
  return 0;
 }