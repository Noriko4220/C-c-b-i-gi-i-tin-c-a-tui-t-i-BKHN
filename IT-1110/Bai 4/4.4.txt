#include<stdio.h>
#include<math.h>
#include<conio.h>
int a,b,c,d,e;
int main() {
	printf("a = ");scanf("%d", &a);
	printf("b = ");scanf("%d", &b);
	if(a>0 && b>0){
		if(b>a){
			e= b;
			b= a;
			a= e;
		}
			for(int i= b; i>0; i--){
				if((b%i)== 0 && (a%i)== 0)
				{
					c= i;
					break;
				}
			}
			for(int i= 1; i<(b+1); i++){
				if(((i*a)%b)== 0)
				{
					d= i*a;
					break;
				}
			}
	} else {
		return 0;
	}
	printf("Uoc chung lon nhat = %d \n", c);
	printf("Boi chung nho nhat = %d", d);
	return 0;
}