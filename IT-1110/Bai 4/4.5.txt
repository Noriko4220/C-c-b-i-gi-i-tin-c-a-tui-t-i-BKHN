#include<stdio.h>
#include<math.h>
#include<conio.h>
double a,b,c,d,e,delta,interMed;
int main() {
	printf("a = ");scanf("%lf", &a);
	printf("b = ");scanf("%lf", &b);
	printf("c = ");scanf("%lf", &c);
	if(a!= 0){
		delta = pow(b,2)-4*a*c;
		d = (-b+sqrt(delta))/(2*a);
		e = (-b-sqrt(delta))/(2*a);
		if(e>d){
			interMed= e;
			e= d;
			d= interMed;
		}
		printf("nghiem 1 = %lf \n", d);
		printf("nghiem 2 = %lf", e);
	} else {
		if (b!= 0){
		printf("nghiem = %lf", -c/b);
	    } else {
	    	if (c==0){
			printf("vo so nghiem");
			} else {
		    printf("vo nghiem");}		
	}
  }
  return 0;
}